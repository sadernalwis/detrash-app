pre-commit:
  parallel: true
  commands:
    eslint:
      glob: '*.{js,ts,jsx,tsx}'
      run: npm lint {staged_files}

commit-msg:
  parallel: true
  commands:
    scripts:
      commitlint.sh:
        runner: bash
